# 本地独立运行 profile：对接 docker-compose.local.yml 起的 PostgreSQL。
# 以 `--spring.profiles.active=local` 激活；值与默认一致，显式留档便于本地调试覆盖。
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security
    username: security
    password: security
